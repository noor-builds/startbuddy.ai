{
  "version": 2,
  "buildCommand": "./flutter/bin/flutter build web --release",
  "installCommand": "git clone --depth 1 --branch stable https://github.com/flutter/flutter.git && ./flutter/bin/flutter config --enable-web",
  "outputDirectory": "build/web"
}